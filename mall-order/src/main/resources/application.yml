spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://42.192.181.137:3306/mall_oms?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root

# MapperScan
# sql映射文件位置
# classpath 和 classpath* 区别：
# classpath：只会到你的class路径中查找找文件;
# classpath*：不仅包含class路径，还包括jar文件中(class路径)进行查找
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto

server:
  port: 9000